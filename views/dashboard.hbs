<div class="flex">
    <div class="hero-spacing">
        <h3 class="h3">Welcome {{name}}</h3>
        {{!-- <a href="/goals/" class="btn btn-float">Set Weekly Goals</a> --}}
    </div>

        {{!-- <table>
            <thead>
                <tr>
                    <th>This Week</th>
                    <th>Current Top Week</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>{{getThisWeekNum}} entries</td>
                    <td>{{getTopWeekDate}}</td>
                    <td>{{stories.length}} entries</td>
                </tr>
            </tbody>
        </table> --}}

    
        <div class="col s4 margin-auto hero-card">
            <div class="card small dash-card">
                <h3>Entries</h3>

                <div class="section">
                    <p class="lead">My daily accomplishments and lessons learned.</p>
                </div>

                <div class="divider"></div>

                <div class="section">
                    <a href="/entries/add" class="btn"><i class="fas fa-plus"></i> Add Entry</a>
                </div>
            </div>
        </div>
</div>

<div class="flex">
    <div class="hero-spacing">
        <h3>&nbsp</h3>
    </div>

            <div class="col s4 margin-auto sub-card">
                <div class="card small dash-card">
                    <h3>Classes</h3>

                    <div class="section">
                        <p class="lead">Short movement classes, for everyone. Repeat and build on your progress.</p>
                    </div>

                    <div class="divider"></div>

                    <div class="section">
                        <a href="/classes/" class="btn">
                        Classes
                        </a>
                    </div>
                </div>  
            </div>

            <div class="col s4 margin-auto sub-card">
                <div class="card small dash-card">
                    <h3>Got a Minute?</h3>

                    <div class="section">
                        <p class="lead">One-minute postures designed to boost your morale or restore/relax.</p>
                    </div>

                    <div class="divider"></div>

                    <div class="section">
                        <a href="/postures/" class="btn">
                        Postures
                        </a>
                    </div>
                </div>
            </div>
</div>



<h5>My Daily Entries</h5>

{{#if stories}}
    <table class="striped">
        <thead>
            <tr>
                <th>Title</th>
                <th>Date</th>
                <th>Status</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            {{#each stories}}
            <tr>
                <td><a href='/entries/{{_id}}'>{{title}}</a></td>
                <td>{{formatDate createdAt 'MMMM Do YYYY'}}</td>
                <td><span class="dash-status">{{status}}</span></td>
                <td>
                    <a href="/entries/edit/{{_id}}" class="btn btn-float">
                        <i class="fas fa-edit"></i>
                    </a>

                    <form action="/entries/{{_id}}" method="POST" id="delete-form">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn orange">
                            <i class="fas fa-trash"></i>
                        </button>
                    </form>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    
{{else}}
    <p>You have not created any entries</p>
{{/if}}